// 1️⃣ Define the sleep function first
async function sleep(millis) {
    return new Promise(resolve => setTimeout(resolve, millis));
}

// 2️⃣ Use it
let t = Date.now();
sleep(100).then(() => console.log(Date.now() - t)); // ~100ms
