# Read number of English subscribers
n = int(input())
english = set(map(int, input().split()))

# Read number of French subscribers
m = int(input())
french = set(map(int, input().split()))

# Find union and print count
result = english.union(french)
print(len(result))